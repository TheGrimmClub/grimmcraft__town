"""Scout — reads Minecraft world data, especially command blocks.

Scans a world's Anvil region files (via ``minecraft.region``) to inventory
block entities and extract every command block with its command and location.
Commands: ``scout scan`` / ``scout commands`` / ``scout report`` / ``scout info``.
"""

__version__ = "2026.1.0"
